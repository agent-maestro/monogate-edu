# Simulation Notes

Create a simulator that maps switch patterns into normalized input frames.

Example:

```text
0000 -> pot_raw 0.000000
1000 -> pot_raw 0.533333
1111 -> pot_raw 1.000000
```

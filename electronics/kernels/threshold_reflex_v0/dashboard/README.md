# Dashboard Notes

The dashboard for this starter kernel should be projection-only:

- read JSONL trace frames;
- display input, requested output, safe output, and guard action;
- highlight meaningful signal changes;
- avoid flashing for timestamp-only changes.

Keep the first dashboard static and dependency-light.
